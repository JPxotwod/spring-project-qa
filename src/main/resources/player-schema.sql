create database project;
use project;
create table player (id integer AUTO_INCREMENT, name varchar(40), position varchar(40), team varchar(40), number integer(11), primary key (id));

select * from player;